<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tms.lesson01.musicgalleryapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicGaleryApplication">
        <activity
            android:name="com.tms.lesson01.musicgalleryapplication.mvvm.MainActivityView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.tms.lesson01.musicgalleryapplication.success.SuccessLoginActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>